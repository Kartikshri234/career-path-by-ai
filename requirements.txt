# CareerCompass — Python dependencies
# Install with: pip install -r requirements.txt

# Core framework
Django>=4.2,<5.2

# Environment variable management
python-decouple==3.8

# Production WSGI server (required by Render)
gunicorn==21.2.0

# Serve static files in production without a separate web server
whitenoise==6.6.0
