# CareerCompass — Python dependencies
# Install with: pip install -r requirements.txt

# Core framework
Django>=4.2,<5.2

# Environment variable management
python-decouple==3.8

# Production WSGI server (required by Render)
gunicorn==21.2.0

# Serve static files in production without a separate web server
whitenoise==6.6.0

# Resume screener dependencies
PyMuPDF>=1.23.0          # PDF text extraction (fitz)
python-docx>=1.1.0        # DOCX text extraction
scikit-learn>=1.4.0       # TF-IDF + cosine similarity
werkzeug>=3.0.0           # secure_filename utility
